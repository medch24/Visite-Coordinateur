{
  "name": "teacher-evaluation-system",
  "version": "1.0.0",
  "description": "A web application for teacher evaluation - Vercel serverless version",
  "main": "index.html",
  "scripts": {
    "build": "echo 'Static build completed'",
    "start": "echo 'Static site - no start needed'"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}