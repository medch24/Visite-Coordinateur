{
  "name": "teacher-evaluation-system",
  "version": "1.0.0",
  "description": "A web application for teacher evaluation - Vercel serverless version",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Build completed - serverless functions ready'"
  },
  "dependencies": {
    "mongoose": "^7.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}